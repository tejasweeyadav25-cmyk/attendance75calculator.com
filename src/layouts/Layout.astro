---
interface Alternate {
	hreflang: string;
	href: string;
}

interface Props {
	title?: string;
	description?: string;
	keywords?: string;
	canonicalUrl?: string;
	ogImage?: string;
	lang?: string;
	ogLocale?: string;
	alternates?: Alternate[];
	themeDarkLabel?: string;
	themeLightLabel?: string;
}

const {
	title = "Attendance Calculator - Free 75% College Attendance Calculator App",
	description = "Free online attendance calculator to check your 75% attendance criteria. Calculate safe bunks, classes needed to reach 75 percent attendance, and semester projections instantly.",
	keywords = "attendance calculator, 75 attendance calculator, attendance calculator 75, attendance calculator app, 75 percent attendance calculator, college attendance calculator, attendance calculator college, student attendance calculator, monthly attendance calculator, attendance calculator 80, attendance calculator 100 percent",
	canonicalUrl = "https://attendance75calculator.com/",
	ogImage = "https://attendance75calculator.com/og-image.png",
	lang = "en",
	ogLocale = "en_US",
	alternates = [],
	// Localized theme-toggle words, read by HomePage's client script via
	// document.documentElement.dataset.themeDarkLabel/themeLightLabel so the
	// label stays in the page's language after the first click (not just on
	// the initial SSR paint). Defaults keep non-localized callers unaffected.
	themeDarkLabel = "Dark",
	themeLightLabel = "Light"
} = Astro.props;

---

<!doctype html>
<html lang={lang} data-theme-dark-label={themeDarkLabel} data-theme-light-label={themeLightLabel}>
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0" />
		<link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="shortcut icon" href="/favicon.ico" />
		<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
		<meta name="apple-mobile-web-app-title" content="BunkCalc" />
		<link rel="manifest" href="/site.webmanifest" />
		<link rel="canonical" href={canonicalUrl} />

		<!-- hreflang alternates for multi-language SEO -->
		{alternates.map((alt) => (
			<link rel="alternate" hreflang={alt.hreflang} href={alt.href} />
		))}

		<!-- Google tag (gtag.js) -->
		<script async src="https://www.googletagmanager.com/gtag/js?id=G-3D0S6Y0XQG" is:inline></script>
		<script is:inline>
			window.dataLayer = window.dataLayer || [];
			function gtag(){dataLayer.push(arguments);}
			gtag('js', new Date());

			gtag('config', 'G-3D0S6Y0XQG');
		</script>

		<!-- Primary Meta Tags -->
		<title>{title}</title>
		<meta name="title" content={title} />
		<meta name="description" content={description} />
		<meta name="keywords" content={keywords} />
		<meta name="author" content="Attendance 75 Calculator" />
		<meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
		<meta name="theme-color" content="#171717" media="(prefers-color-scheme: dark)" />
		<meta name="theme-color" content="#fafafa" media="(prefers-color-scheme: light)" />

		<!-- Open Graph / Facebook -->
		<meta property="og:type" content="website" />
		<meta property="og:url" content={canonicalUrl} />
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={ogImage} />
		<meta property="og:image:width" content="1200" />
		<meta property="og:image:height" content="630" />
		<meta property="og:image:type" content="image/png" />
		<meta property="og:image:alt" content="Attendance Calculator - 75 Percent Attendance Tracker" />
		<meta property="og:site_name" content="Attendance 75 Calculator" />
		<meta property="og:locale" content={ogLocale} />

		<!-- Twitter -->
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:url" content={canonicalUrl} />
		<meta name="twitter:title" content={title} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={ogImage} />
		<meta name="twitter:image:alt" content="Attendance Calculator - 75 Percent Attendance Tracker" />

		<!-- Page-specific JSON-LD Structured Data Schema -->
		<slot name="head-extra" />

		<!-- Theme Initialization Script to prevent Flash of Light/Dark Theme -->
		<script is:inline>
			(function() {
				const savedTheme = localStorage.getItem('theme');
				if (savedTheme === 'dark' || (!savedTheme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
					document.documentElement.classList.add('dark');
				} else {
					document.documentElement.classList.remove('dark');
				}
			})();
		</script>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	@import '../styles/app.css';
</style>
